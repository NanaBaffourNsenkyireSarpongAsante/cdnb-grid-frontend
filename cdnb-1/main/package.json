{
  "name": "cdnb-grid",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "CDNB smart-farm grid — Vite frontend + zero-dependency SQLite ingestion API",
  "scripts": {
    "dev": "node dev.mjs",
    "dev:api": "node --no-warnings server/index.js",
    "dev:web": "npm --prefix frontend run dev",
    "seed:reset": "node -e \"require('fs').rmSync('server/data', {recursive:true, force:true}); console.log('db removed — it will be re-seeded on next server start')\""
  }
}
